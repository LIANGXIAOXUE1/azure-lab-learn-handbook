# 创建 Windows VM

## 1. 是什么

Windows VM（Windows Virtual Machine）就是运行在 Azure 云上的一台 Windows 虚拟机。

你可以把它先简单理解成：

> 一台放在 Azure 数据中心里的远程 Windows 电脑 / 服务器。

它和你平时接触的本地 Windows 电脑有些类似，但区别在于：

- 它运行在云上
- 你可以按需创建和删除
- 你可以远程连接进去使用
- 你不需要自己购买物理服务器

在 Azure 里，VM 是最基础、也最常见的计算资源之一。

---

## 2. 解决什么问题

创建 Windows VM 主要是为了解决下面这些问题：

### 2.1 需要一台可远程登录的 Windows 服务器
很多业务系统、管理工具、传统应用，需要运行在 Windows 环境中，这时就可以使用 Windows VM。

### 2.2 不想自己采购和维护本地服务器
如果使用本地服务器，通常要自己采购硬件、安装系统、维护环境。  
Azure VM 可以直接按需创建，节省前期准备成本。

### 2.3 需要快速搭建测试或演示环境
比如你想临时做一个：

- Windows 测试机
- 软件安装实验环境
- 客户演示环境
- 运维跳板机

都可以快速通过 VM 搭起来。

### 2.4 需要更灵活的资源使用方式
本地服务器一旦买下来，扩容和回收都不灵活。  
VM 则可以根据需要调整规格，实验结束后也可以直接删除。

---

## 3. 什么时候会用到 Windows VM

Windows VM 常见使用场景包括：

- 部署依赖 Windows Server 的业务系统
- 运行传统 .NET 应用或老系统
- 做远程桌面运维管理
- 搭建堡垒机 / 跳板机
- 做学习实验和环境验证
- 临时给客户演示某套系统

对初学 Azure 的人来说，创建 Windows VM 也是一个非常好的入门实验，因为它会把很多基础概念串起来：

- Subscription
- Resource Group
- Region
- VM Size
- Username / Password
- Public IP
- NSG
- RDP 远程连接

---

## 4. 创建 Windows VM 前先理解什么

在真正创建之前，建议先知道下面这些概念。

### 4.1 VM 只是计算资源，不是完整方案
创建一台 VM，不代表整个环境就齐了。  
通常还会涉及：

- 虚拟网络（VNet）
- 子网（Subnet）
- 公网 IP（Public IP）
- 网络安全组（NSG）
- 磁盘（Disk）

### 4.2 能创建成功，不代表能连得上
很多初学者会以为 VM 创建完成就结束了。  
其实 VM 创建成功只是第一步，后面还要验证：

- 账号密码是否正确
- 公网 IP 是否可访问
- NSG 是否放通 RDP 3389
- 本机网络是否允许出站访问
- Windows 系统是否正常启动

### 4.3 VM 会产生费用
只要 VM 在运行，通常就会持续产生费用。  
所以做实验时要特别注意：

- 不用时可以停止
- 实验结束可以删除
- 避免忘记清理资源

---

## 5. 怎么创建

下面以 Azure Portal 为例，演示最基础的 Windows VM 创建流程。

### 5.1 进入创建页面

1. 登录 Azure Portal
2. 搜索 `Virtual machines`
3. 点击 `Create`
4. 选择 `Azure virtual machine`

---

### 5.2 基础信息（Basics）

在 `Basics` 页签中，通常需要填写这些内容：

#### Subscription
选择资源所属的订阅。

#### Resource Group
选择已有资源组，或者新建一个资源组，例如：

```text
rg-azure-learning-vm
```

#### Virtual machine name
输入虚拟机名称，例如：

```text
vm-win-learn-01
```

#### Region
选择部署区域，例如：

- East Asia
- Southeast Asia
- Japan East

#### Availability options
学习阶段可以先用默认选项。

#### Image
选择 Windows 镜像，例如：

- Windows Server 2022 Datacenter
- Windows Server 2019 Datacenter

#### Size
选择 VM 规格，例如适合学习测试的小规格。

#### Administrator account
设置管理员用户名和密码。

> 注意：用户名和密码必须满足 Azure 的复杂度要求。

---

### 5.3 入站端口（Inbound port rules）

如果你希望后续通过远程桌面连接这台 Windows VM，通常需要允许：

- **RDP (3389)**

常见做法是：

- 选择 `Allow selected ports`
- 勾选 `RDP (3389)`

> 学习测试可以这样做，但正式环境需要更谨慎，避免公网直接暴露。

---

### 5.4 磁盘（Disks）

一般保持默认即可，学习阶段重点先放在：

- VM 能否成功创建
- 能否成功连接
- 能否理解资源关系

---

### 5.5 网络（Networking）

这里通常会涉及：

- Virtual Network
- Subnet
- Public IP
- NIC network security group

如果你没有提前创建网络资源，Azure 往往会帮你自动生成默认资源。

学习阶段先重点观察：

- 是否分配了 Public IP
- 是否创建了 NSG
- 是否允许了 RDP

---

### 5.6 检查并创建（Review + create）

1. 填完配置后，点击 `Review + create`
2. 等待 Azure 校验配置
3. 如果没有报错，点击 `Create`

然后等待部署完成。

---

## 6. 创建后会同时生成什么资源

很多初学者会以为“我只是创建了一台 VM”，但实际上 Azure 往往会一起生成多个相关资源。

常见包括：

- Virtual Machine
- Network Interface（NIC）
- Public IP
- Network Security Group（有时会自动创建）
- Virtual Network / Subnet（如果之前没有）
- OS Disk

所以建议你创建完成后，去资源组里看一下资源清单。  
这样你会更清楚：

> 创建一台 VM，不只是生成一个单独的“服务器对象”，而是一组相关资源的组合。

---

## 7. 怎么测试

创建完成后，建议做下面几个基础测试。

### 7.1 测试 1：确认 VM 创建成功

进入 VM 详情页，检查：

- VM 状态是否为 `Running`
- 是否有 Public IP
- 是否显示操作系统为 Windows

---

### 7.2 测试 2：检查 RDP 端口是否已放通

查看：

- VM 的 Networking 页面
- NSG 入站规则
- 是否允许 TCP 3389

如果没有放通，你后面通常无法用远程桌面连接。

---

### 7.3 测试 3：使用远程桌面连接（RDP）

在 Windows 本机上：

1. 打开“远程桌面连接”
2. 输入 VM 的 Public IP
3. 输入你创建 VM 时设置的管理员用户名和密码
4. 尝试登录

如果能成功进入 Windows 桌面，说明基础创建和连接都成功了。

---

### 7.4 测试 4：重启与停止

可以尝试：

- Restart
- Stop
- Start

观察 VM 状态变化，理解它的生命周期管理方式。

---

### 7.5 测试 5：删除资源组

如果这是测试环境，实验完成后可以删除整个资源组，确认相关资源是否被一起删除。

---

## 8. 这个实验想让你理解什么

创建 Windows VM 不只是“会点 Portal”，更重要的是理解这些事：

### 8.1 Azure 中的一个资源通常会带出一组配套资源
VM 背后往往不只是 VM 本体，还包括网卡、磁盘、公网 IP、NSG 等。

### 8.2 云上服务器不是创建完就结束
后续还要关注：

- 登录方式
- 网络放通
- 密码管理
- 安全配置
- 成本控制

### 8.3 计算、网络、安全是连在一起的
你后面连不上 VM，很多时候不是“机器坏了”，而是：

- IP 没分配好
- NSG 没放通
- 用户名密码不对
- 本地网络有限制

---

## 9. 常见问题

### 问题 1：VM 创建成功了，为什么连不上？

常见原因包括：

- 没有 Public IP
- NSG 没有放通 3389
- 用户名或密码错误
- 本地网络限制了 RDP
- VM 还没完全启动好
- Windows 防火墙或系统状态异常

---

### 问题 2：创建 VM 为什么会自动多出很多资源？

因为 VM 本身需要依赖其他资源一起工作，例如：

- 网卡
- 磁盘
- 公网 IP
- 虚拟网络
- NSG

所以你看到多个资源是正常的。

---

### 问题 3：Windows VM 和本地电脑有什么区别？

本地电脑通常在你自己手边；  
Windows VM 是运行在 Azure 云上的远程 Windows 系统，需要通过网络远程连接。

它更像一台云上的服务器，而不是一台本地办公电脑。

---

### 问题 4：不用的时候关机就不会收费了吗？

不一定。  
要区分：

- 在 Windows 系统里点击关机
- 在 Azure Portal 里执行 Stop / Deallocate

很多时候只有在 Azure 里真正停止并释放计算资源后，计算费用才会停止。

不过像磁盘、公网 IP 等资源可能仍然继续计费，所以实验结束后最好确认资源状态，必要时直接删除。

---

### 问题 5：学习阶段该选多大规格？

学习和测试阶段通常选择较小规格即可，只要能完成创建、连接和基础实验就行。  
不需要一开始就选很大的配置，否则更容易产生不必要费用。

---

## 10. 客户常问的问题

这一部分重点不是只记录客户怎么问，而是练习你该怎么回答。

---

### 问题 1：Azure VM 是不是就是云服务器？

**简短回答：**  
可以这样理解。Azure VM 本质上就是运行在 Azure 云上的虚拟服务器。

**展开回答：**  
如果客户需要一台自己可以控制操作系统、安装软件、远程登录管理的服务器，那么 Azure VM 就是很典型的选择。  
它和传统服务器类似，但不需要自己买物理硬件，而是按需在云上开通。

---

### 问题 2：什么时候该用 VM？

**简短回答：**  
当你需要操作系统级别控制时，VM 是比较合适的选择。

**展开回答：**  
例如这些场景更适合 VM：

- 需要安装特定软件
- 需要远程桌面登录
- 需要运行传统 Windows 应用
- 需要对系统层做较多自定义配置

如果客户只是想托管网站或 API，不一定非要上 VM，也可能更适合 App Service 这类平台服务。

---

### 问题 3：VM 和 App Service 怎么选？

**简短回答：**  
如果你要更多系统控制权，选 VM；如果你更想少管运维，优先看 App Service。

**展开回答：**  
VM 的优点是灵活，可以自己管理操作系统和环境。  
但缺点是要自己承担更多运维工作。

App Service 更适合：

- 网站
- Web API
- 常规应用托管

它的优势是平台帮你管更多底层细节。  
所以到底怎么选，关键看客户想要的是“控制力”还是“省运维”。

---

### 问题 4：Windows VM 安全吗？

**简短回答：**  
Azure 提供很多安全能力，但是否安全还取决于访问控制、网络配置和运维方式。

**展开回答：**  
如果一台 VM 直接暴露公网，又弱密码、权限配置随意，那肯定有风险。  
安全通常要结合：

- 强密码
- 最小权限
- NSG 限制来源 IP
- 关闭不必要端口
- 补丁更新
- 日志审计

所以“平台有安全能力”不等于“默认就绝对安全”。

---

### 问题 5：不用的时候怎么控成本？

**简短回答：**  
可以通过停止、降规格、删除测试资源等方式控制成本。

**展开回答：**  
例如：

- 实验环境不用时及时停止
- 长期不用直接删除
- 不要选择过大的 VM 规格
- 区分测试环境和生产环境
- 定期检查有没有遗留的磁盘、公网 IP、备份资源

这样可以避免很多不必要开销。

---

## 11. 你可以怎么对客户解释

如果客户第一次接触 Azure VM，不建议一开始就讲太多参数和规格。  
可以先从价值角度解释。

你可以这样说：

> Azure VM 可以理解为一台运行在云上的远程服务器。  
> 如果客户需要自己控制操作系统、安装软件、远程登录管理环境，那么 VM 是很常见的选择。  
> 它的优势是灵活、上线快，不需要自己采购物理服务器。  
> 但同时也意味着客户要自己承担更多系统层面的管理工作，比如账号、补丁、端口和安全配置。  
> 所以 VM 更适合需要较高控制权的场景，而如果只是部署网站或 API，也可以进一步考虑 App Service 这类更省运维的平台服务。

---

## 12. 实践建议

学习完这一篇后，建议至少做下面几个动作：

### 12.1 创建一台测试用 Windows VM
尽量命名清晰，例如：

```text
vm-win-learn-01
```

### 12.2 记录创建时选了哪些关键参数
例如：

- Subscription
- Resource Group
- Region
- Image
- Size
- Username
- Public IP
- RDP 是否放通

### 12.3 创建完成后去资源组里看资源清单
确认是不是除了 VM 之外，还多出了：

- NIC
- Disk
- Public IP
- NSG
- VNet

### 12.4 尝试 RDP 登录
实际远程连接一次，确认这不是“纸面创建成功”，而是真正可访问。

### 12.5 做一次停止和删除实验
理解成本和生命周期管理。

---

## 13. 实践记录

- **使用的订阅：**
- **资源组名称：**
- **VM 名称：**
- **部署区域：**
- **镜像版本：**
- **VM 规格：**
- **管理员用户名：**
- **是否配置 Public IP：**
- **是否放通 RDP 3389：**
- **是否成功 RDP 登录：**
- **资源组中自动创建了哪些配套资源：**
- **是否测试了停止 / 启动：**
- **是否测试了删除资源组：**
- **遇到的问题：**
- **解决方式：**
- **相关截图：** 后续补充

---

## 14. 本篇总结

Windows VM 是 Azure 中最基础的计算资源之一。  
它的核心价值是：在云上快速提供一台可以远程管理的 Windows 服务器。

学习这一步时，真正重要的不只是“创建成功”，而是理解：

- VM 需要放在哪个 Subscription / Resource Group / Region
- VM 背后通常会带出网络、磁盘和安全相关资源
- 创建成功后还要验证连接、账号、端口和安全配置
- VM 灵活，但也意味着你要承担更多运维责任
- 学习和测试环境要注意及时停机或删除，避免浪费成本
 
